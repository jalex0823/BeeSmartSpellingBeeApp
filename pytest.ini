[pytest]
# Keep CI/dev runs stable by only collecting intended tests.
# The repo also contains root-level scripts named test_*.py that are interactive
# or integration harnesses and may call exit(), require auth, or require live services.

testpaths = tests
python_files = test_*.py
addopts = -ra
